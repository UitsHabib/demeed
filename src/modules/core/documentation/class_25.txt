1. all the schema validation will be in one file.

2. one file must have one responsibility, one function must have one responsibility.

3. middleware function:

    1. modules -> core -> middlewares -> validator.middleware.js : This validator will validate the email,password,confirmPassword etc. And it will be inside the routes file and work as a middleware. for furthur information check the middlewareTest.js file in the documentation folder.

    2. all common middleware will be inside the core in the middlewares folder.
